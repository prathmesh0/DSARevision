#include <bits/stdc++.h>
using namespace std;

bool twoSum(vector<int> &arr, int target)
{
    // int n = arr.size();
    // for (int i = 0; i < n; i++)
    // {

    //     for (int j = i + 1; j < n; j++)
    //     {
    //         if (arr[i] + arr[j] == target)

    //             return true;
    //     }
    // }
    // return false;

    // better approach
    int n = 0;
    int mpp[n];
    for(int i = 0; i<n; i++){
        sum += arr[i];
        mpp[]
    }
}

int main()
{
    vector<int> arr = {2, 6, 5, 8, 11};
    int target = 14;
    // first variant

    bool ans = twoSum(arr, target);
    if (ans)
    {
        cout << "sum is founded" << endl;
    }
    else
    {
        cout << "sum is not founded" << endl;
    }
    return 0;
}